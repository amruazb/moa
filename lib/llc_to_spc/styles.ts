// Re-export styles from SPC MOA since they are mostly identical
// LLC to SPC conversion uses the same styling infrastructure
export { generateMoaStyles, getBaseFontSize, moaStyles } from '../spc_moa/styles'
